revert: Revert changes to AI models and to-do list

Reverts the changes made to the AI models in `content.py` and the to-do list in `todo.md`. These changes were made in an attempt to fix a deployment issue that is still unresolved.