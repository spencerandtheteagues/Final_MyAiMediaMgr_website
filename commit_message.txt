fix(frontend): Unify social media icons in Dashboard

Updates the Dashboard component to use the new, consistent SVG social media icons, replacing the placeholder text avatars. This ensures all brand logos are consistent across the application.